@using PagedList;
@using PagedList.Mvc;
@model PagedList.IPagedList<Website_Dien_Thoai.Models.ThuongHieu>
@{
    ViewBag.Title = "lstthuonghieu";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>lstthuonghieu</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().maTH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().tenTH)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.maTH)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tenTH)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.maTH }) |
                @Html.ActionLink("Details", "Details", new { id = item.maTH }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.maTH })
            </td>
        </tr>
    }

</table>
Trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber ) of @Model.PageCount
@Html.PagedListPager(Model, page => Url.Action("lstthuonghieu", new { page, currentFilter = ViewBag.CurrentFilter }))